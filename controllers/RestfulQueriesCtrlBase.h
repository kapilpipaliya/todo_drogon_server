/**
 *
 *  RestfulQueriesCtrlBase.h
 *  DO NOT EDIT. This file is generated by drogon_ctl automatically.
 *  Users should implement business logic in the derived class.
 */

#pragma once

#include <drogon/HttpController.h>

#include "Queries.h"
using namespace drogon;

using namespace drogon_model::openproject6;

/**
 * @brief this class is created by the drogon_ctl command.
 * this class is a restful API controller for reading and writing the queries table.
 */

class RestfulQueriesCtrlBase
{
  public:
    void getOne(const HttpRequestPtr &req,
                std::function<void(const HttpResponsePtr &)> &&callback,
                Queries::PrimaryKeyType &&id);
    void updateOne(const HttpRequestPtr &req,
                   std::function<void(const HttpResponsePtr &)> &&callback,
                   Queries::PrimaryKeyType &&id);
    void deleteOne(const HttpRequestPtr &req,
                   std::function<void(const HttpResponsePtr &)> &&callback,
                   Queries::PrimaryKeyType &&id);
    void get(const HttpRequestPtr &req,
             std::function<void(const HttpResponsePtr &)> &&callback);
    void create(const HttpRequestPtr &req,
                std::function<void(const HttpResponsePtr &)> &&callback);


//    void update(const HttpRequestPtr &req,
//                std::function<void(const HttpResponsePtr &)> &&callback);

    orm::DbClientPtr getDbClient() 
    {
        return drogon::app().getDbClient(_dbClientName);
    }
    void enableMasquerading(const std::vector<std::string> &pMasqueradingVector)
    {
        _masquerading = true;
        _masqueradingVector = pMasqueradingVector;
    }
    void disableMasquerading()
    {
        _masquerading = false;
    }
  protected:
    const std::string _dbClientName = "sce";
    bool _masquerading = true;
    /**
     * The items in the vector are aliases of column names in the table.
     * if one item is set to an empty string, the related column is not sent to clients.
     */
    std::vector<std::string> _masqueradingVector={
          "id", // the alias for the id column.
          "project_id", // the alias for the project_id column.
          "name", // the alias for the name column.
          "filters", // the alias for the filters column.
          "user_id", // the alias for the user_id column.
          "is_public", // the alias for the is_public column.
          "column_names", // the alias for the column_names column.
          "sort_criteria", // the alias for the sort_criteria column.
          "group_by", // the alias for the group_by column.
          "display_sums", // the alias for the display_sums column.
          "timeline_visible", // the alias for the timeline_visible column.
          "show_hierarchies", // the alias for the show_hierarchies column.
          "timeline_zoom_level", // the alias for the timeline_zoom_level column.
          "timeline_labels", // the alias for the timeline_labels column.
          "highlighting_mode", // the alias for the highlighting_mode column.
          "highlighted_attributes", // the alias for the highlighted_attributes column.
          "hidden", // the alias for the hidden column.
          "created_at", // the alias for the created_at column.
          "updated_at", // the alias for the updated_at column.
          "display_representation"  // the alias for the display_representation column.
    };
};
