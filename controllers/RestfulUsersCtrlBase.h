/**
 *
 *  RestfulUsersCtrlBase.h
 *  DO NOT EDIT. This file is generated by drogon_ctl automatically.
 *  Users should implement business logic in the derived class.
 */

#pragma once

#include <drogon/HttpController.h>

#include "Users.h"
using namespace drogon;

using namespace drogon_model::openproject6;

/**
 * @brief this class is created by the drogon_ctl command.
 * this class is a restful API controller for reading and writing the users table.
 */

class RestfulUsersCtrlBase
{
  public:
    void getOne(const HttpRequestPtr &req,
                std::function<void(const HttpResponsePtr &)> &&callback,
                Users::PrimaryKeyType &&id);
    void updateOne(const HttpRequestPtr &req,
                   std::function<void(const HttpResponsePtr &)> &&callback,
                   Users::PrimaryKeyType &&id);
    void deleteOne(const HttpRequestPtr &req,
                   std::function<void(const HttpResponsePtr &)> &&callback,
                   Users::PrimaryKeyType &&id);
    void get(const HttpRequestPtr &req,
             std::function<void(const HttpResponsePtr &)> &&callback);
    void create(const HttpRequestPtr &req,
                std::function<void(const HttpResponsePtr &)> &&callback);


//    void update(const HttpRequestPtr &req,
//                std::function<void(const HttpResponsePtr &)> &&callback);

    orm::DbClientPtr getDbClient() 
    {
        return drogon::app().getDbClient(_dbClientName);
    }
    void enableMasquerading(const std::vector<std::string> &pMasqueradingVector)
    {
        _masquerading = true;
        _masqueradingVector = pMasqueradingVector;
    }
    void disableMasquerading()
    {
        _masquerading = false;
    }
  protected:
    const std::string _dbClientName = "sce";
    bool _masquerading = true;
    /**
     * The items in the vector are aliases of column names in the table.
     * if one item is set to an empty string, the related column is not sent to clients.
     */
    std::vector<std::string> _masqueradingVector={
          "id", // the alias for the id column.
          "login", // the alias for the login column.
          "firstname", // the alias for the firstname column.
          "lastname", // the alias for the lastname column.
          "mail", // the alias for the mail column.
          "admin", // the alias for the admin column.
          "status", // the alias for the status column.
          "last_login_on", // the alias for the last_login_on column.
          "language", // the alias for the language column.
          "auth_source_id", // the alias for the auth_source_id column.
          "created_on", // the alias for the created_on column.
          "updated_on", // the alias for the updated_on column.
          "type", // the alias for the type column.
          "identity_url", // the alias for the identity_url column.
          "mail_notification", // the alias for the mail_notification column.
          "first_login", // the alias for the first_login column.
          "force_password_change", // the alias for the force_password_change column.
          "failed_login_count", // the alias for the failed_login_count column.
          "last_failed_login_on", // the alias for the last_failed_login_on column.
          "consented_at"  // the alias for the consented_at column.
    };
};
